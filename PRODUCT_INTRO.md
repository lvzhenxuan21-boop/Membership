# Membership Pro 1.1 — 可售卖版 Laravel 多租户会员 + 商城 SaaS 源码

**一句话定位：一套源码，同时给你「多租户 SaaS 收租」和「商城 + 全套会员体系」两种生意模式，开箱即售，3 分钟跑起来。**

## 这是什么

基于 Laravel 13 + PHP 8.3 + Filament 5 的多租户会员系统源码。你买到的不是 demo 拼装，而是一套数据按租户隔离、会员机制真实生效、支付可上生产、经过 71 个自动化测试验证的完整商业系统。你可以拿它：①自己做 SaaS 平台，招商户入驻、按订单抽佣（内置 5% 抽佣和平台佣金看板：累计/本月/今日/GMV/7 天趋势）；②直接交付给实体店/连锁客户当会员系统；③二次开发成垂直行业产品。

## 核心功能（21 张业务表全闭环）

- **多租户 SaaS**：一套代码承载 N 家商户，后台数据全链路租户隔离（全局作用域 + 写入守护 + 面板准入 + Model Policy 四层防护），子域名 `shop1.xxx.com` 自动识别商户，含获客首页、30 秒自助开店、开店审核开关（pending 状态 + 后台审核）
- **商城电商**：店铺货架、商品图上传、按店铺隔离的购物车、下单锁库存、订单-支付单全链路、发货/完成操作、平台抽佣、取消/退款自动回补库存与积分
- **会员体系全部真实接线**：等级折扣直接作用于下单、积分倍率入账、积分抵现、储值钱包（bcadd 精度）、优惠券（限领/限用/满减/包邮）、连续签到奖励、订阅到期自动置位 + 邮件提醒、保级周期不达标自动降级并写审计日志
- **权益系统**：套餐绑定配额（quota/布尔/折扣三类），行锁原子扣减
- **支付**：6 渠道工厂。Stripe 完整可用（Checkout + 真实退款 + Webhook 强制验签）；微信/支付宝内置验签逻辑，附 10 分钟接入文档；钱包余额、线下核销开箱可用；生产环境强制禁用 Mock 渠道，晚到款对账兜底，退款语义完整（部分退款/全额冲销）
- **运营后台**：Filament 16 个资源，平台佣金看板、审计日志可视化、资金字段全只读（积分/余额调整走带流水的正规入口）、按角色收紧的资金操作
- **账号与安全**：密码找回（Web + API，防枚举）、可选邮箱验证（开关控制，开启后未验证不能下单）、登录/注册/开店/下单全限流、安全响应头、认证邮件队列化、TrustProxies 支持

## 质量与交付

- **71 个测试 / 194 断言，SQLite 与 MySQL 8 双引擎全绿**——回归有保障，不是"能跑就行"
- 并发认真：下单锁库存、积分/钱包行锁账本、支付核销幂等、优惠券名额防超发、签到同日唯一键防重放
- 交付物：全部源码 + 安装指南 + 生产部署文档（泛域名 DNS/SSL/nginx 可复制配置）+ 支付接入文档 + API 交互文档（`/docs/api`，OpenAPI 3.1）+ CHANGELOG/升级指南 + 演示数据
- **白标只需改 `APP_NAME`**：后台品牌、页面标题、SEO 标签全部跟随配置

## 技术栈与部署

Laravel 13 / PHP 8.3 / Filament 5 / Sanctum / Spatie Permission / Cashier 16；SQLite 开箱即用，改 `.env` 即切 MySQL（已实测）。含调度任务（过期单取消、订阅到期提醒、保级降级），composer.lock 锁定版本。

## 适合谁买

接客户项目的开发者（健身房/门店/连锁会员系统直接交付）、想做会员 SaaS 创业的小团队、需要一套工程质量可靠的 Laravel 电商+会员底座做二次开发的技术人。

**售后：一年 bug 修复更新；可选部署/支付接入/定制服务。**
